version: "3.8"
services:
  backend:
    image: hindiesuputra/hackathon-be
    ports:
      - "3000:3000"

  frontend:
    image: hindiesuputra/hackathon-fe
    ports:
      - "80:80"
    depends_on:
      - backend
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
